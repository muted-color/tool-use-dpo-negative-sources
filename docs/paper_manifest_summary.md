# Paper-Ready Manifest Summary

Use this table to update `paper/main.tex` after the repository manifest is final.
Do not copy values from historical `report/stage4_*_paper_draft*.md`.

| Evaluation surface | Role | Source/split/snapshot | n | Evaluator/parser | Metric definition | Seed/decoding | Released artifact path |
|---|---|---|---:|---|---|---|---|
| BFCL | Primary structural function-calling axis | `gorilla-llm/Berkeley-Function-Calling-Leaderboard@61fc0608cfd831fcfbbaa676ebdfef0ed963eeda`; first 60 each from `BFCL_v3_simple`, `multiple`, `parallel`, `irrelevance`, `live_multiple` | 300 | `stage3d_eval.py` -> `stage1_candidate_eval.py`; `qwen3_tool_call_parser_stage0_policy` | Exact-match BFCL core; category exact-match diagnostics | Deterministic decoding, `do_sample=False`, `enable_thinking=false`; frozen hash manifest | `results/per_example/primary/*_<checkpoint>.csv`, `results/aggregate/main_metrics_*.csv` |
| When2Call | Primary call-decision axis | `nvidia/When2Call@0582f7749df63a96fdc3070932e83e72396ace53`; config `test`, split `mcq`; 100 `tool_call`, 100 `follow_up`, 100 `unable`, 0 `direct_answer` gold rows | 300 | `stage3d_eval.py` -> `stage1_candidate_eval.py` | Behavior accuracy over represented gold labels; frozen evaluator macro F1 includes `direct_answer` as a zero-support class with `zero_division=0` | Deterministic decoding, `do_sample=False`, `enable_thinking=false`; frozen hash manifest | `results/per_example/primary/*_<checkpoint>.csv`, `results/aggregate/main_metrics_*.csv` |
| SFT-dev auxiliary | Auxiliary primary diagnostics | Frozen Stage1 SFT-dev split; 351 `tool_call`, 93 `direct_answer`, 30 `follow_up`, 26 `unable` | 500 | `stage3d_eval.py` -> `stage1_candidate_eval.py` | Strict parse, SFT-dev tool-call accuracy, SFT-dev behavior accuracy, nontrivial-error diagnostics | Deterministic decoding, `do_sample=False`, `enable_thinking=false`; frozen hash manifest | `results/per_example/primary/*_<checkpoint>.csv`, `results/aggregate/main_metrics_*.csv` |
| IFEval-style | Instruction-following guardrail | `google/IFEval@966cd89545d6b6acfd7638bc708b98261ca58e84`; first 100 raw rows from `ifeval_input_data.jsonl`; 96 prompt-level rows have at least one supported instruction | raw 100 / eval 96 | `stage3d_eval.py` -> `stage1_secondary_eval.py` | Prompt-level strict denominator excludes zero-supported prompts; instruction-level accuracy over supported instructions | Deterministic decoding, `do_sample=False`, `enable_thinking=false`; frozen hash manifest | `results/per_example/ifeval/*_<checkpoint>.csv`, `results/aggregate/main_metrics_*.csv` |
| FunctionChat-Bench | Secondary transfer diagnostic | `kakao/FunctionChat-Bench@5ddb0b5bb37d6423e1f3381ef693cda811a7847e`; 25 singlecall, 45 dialog, 30 call_decision | 100 | `stage3d_eval.py` -> `stage1_secondary_eval.py` | Function-name, argument, output-type, and tool-hallucination diagnostics | Deterministic decoding, `do_sample=False`, `enable_thinking=false`; frozen hash manifest | `results/per_example/functionchat/*_<checkpoint>.csv`, `results/aggregate/main_metrics_*.csv` |
| Pairwise bootstrap | Uncertainty for reported deltas | Sanitized primary per-example outputs grouped by shared `prompt_id` | metric-specific | `scripts/compute_grouped_bootstrap.py` | Percentile CI over paired metric deltas | 1000 iterations; seed rule `20260603 + comparison_metric_index * 97` | `artifacts/stage4/bootstrap/pairwise_bootstrap_ci.csv` |

## LLM Audit Manifest Summary

- Stage3d clean-pool holdout gates are LLM quality-control gates, not human annotations. Active clean DPO sources use external `gpt-5_5-2026-04-23` high labels with JSON response format, `reasoning_effort=high`, `max_completion_tokens=4096`, and no client-specified temperature for the OpenAI chat-completions request.
- Stage3d active clean gates passed for `r026` noised_gold and `r027b` behavior, each with 100 holdout rows. The earlier `r027` behavior attempt failed the chosen/reference-suspect gate and is not the DPO training source.
- Stage3b matched diagnostic audit uses local `gpt-oss-120b` (`ggml-org/gpt-oss-120b-GGUF`) first with `temperature=0`, JSON response format, and `max_tokens=4096`, followed by external `gpt-5_5-2026-04-23` high final adjudication on all 300 items. The matched diagnostic audit is source-risk evidence only; it is not human ground truth and does not authorize matched mini-DPO reporting.
