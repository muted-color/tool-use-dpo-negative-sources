from __future__ import annotations

from pathlib import Path


REPO_ROOT = Path(__file__).resolve().parents[2]
ARTIFACTS = REPO_ROOT / "artifacts"
CONFIGS = REPO_ROOT / "configs"
REPRODUCED = REPO_ROOT / "reproduced"

